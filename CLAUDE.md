# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository State

This is a new, empty repository with no source code yet. The only configuration present is `.vscode/settings.json`, which enables Claude Code terminal integration (`claudeCode.useTerminal: true`).

Add project-specific build commands, architecture notes, and development workflows here as the project grows.
