name = "assumption-mapper-worker"
main = "worker.js"
compatibility_date = "2024-09-23"

[vars]
ALLOWED_ORIGIN = "https://matgitrepo.github.io"

# Secrets — set via:
#   wrangler secret put ANTHROPIC_API_KEY
